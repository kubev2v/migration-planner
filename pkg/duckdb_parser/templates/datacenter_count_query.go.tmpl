{{- /*
Datacenter Count Query Template - Returns count of unique datacenters.

Uses vhost table to match old implementation behavior which extracted
datacenter info from the vHost sheet.
*/ -}}
SELECT COUNT(DISTINCT "Datacenter")
FROM vhost
WHERE "Datacenter" IS NOT NULL AND "Datacenter" != '';
